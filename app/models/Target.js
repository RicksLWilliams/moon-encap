export default class Target {
  constructor(data) {
    //console.log("We created BOB the target")

    this.cheese = data.cheese
    this.clickBonus = data.clickBonus
    this.autoBonus = data.autoBonus
    this.upgrades = data.upgrades

  }
}
